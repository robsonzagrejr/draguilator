def howToNotTestPrimes(int n)
{
    string returnMessage;
    if (n == 2)
    {
        string ns;
        ns = n;
        returnMessage = ns + " is prime!";
        print returnMessage;
        return;
    }
    else
    {
        if (n == 3)
        {
            string ns;
            ns = n;
            returnMessage = ns + " is prime!";
            print returnMessage;
            return;
        }
        else
        {
            if (n == 5)
            {
                string ns;
                ns = n;
                returnMessage = ns + " is prime!";
                print returnMessage;
                return;
            }
            else
            {
                if (n == 7)
                {
                    string ns;
                    ns = n;
                    returnMessage = ns + " is prime!";
                    print returnMessage;
                    return;
                }
                else
                {
                    if (n == 11)
                    {
                        string ns;
                        ns = n;
                        returnMessage = ns + " is prime!";
                        print returnMessage;
                        return;
                    }
                    else
                    {
                        if (n == 13)
                        {
                            string ns;
                            ns = n;
                            returnMessage = ns + " is prime!";
                            print returnMessage;
                            return;
                        }
                        else
                        {
                            if (n > 13)
                            {
                                string ns;
                                ns = n;
                                returnMessage = ns + " is probably not prime.";
                                print returnMessage;
                            }
                            else
                            {
                                string ns;
                                ns = n;
                                returnMessage = ns + " is not prime.";
                                print returnMessage;
                                return;
                            }
                        }
                    }
                }
            }
        }
    }
}


def driver() {
    int a;
    int b;
    int c;
    int d;
    int e;
    int f;
    int g;
    int h;
    int i;
    int j;
    a = -5;
    b = -1;
    c = 0;
    d = 1;
    e = 2;
    f = 3;
    g = 4;
    h = 13;
    i = 14;
    j = 20;
    a = howToNotTestPrimes(a);
    b = howToNotTestPrimes(b);
    c = howToNotTestPrimes(c);
    d = howToNotTestPrimes(d);
    e = howToNotTestPrimes(e);
    f = howToNotTestPrimes(f);
    g =howToNotTestPrimes(g);
    h = howToNotTestPrimes(h);
    i = howToNotTestPrimes(i);
    j = howToNotTestPrimes(j);
}
